<label for="title">Title</label>
  <input type="text" name = "post[title]" value="<%= @post.try(:title) %>"><br />

<label for="body">Body</label>
  <textarea name= "post[body]" value="<%= @post.try(:body) %>"></textarea><br />